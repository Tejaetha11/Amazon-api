{
  "name": "amazon-api",
  "version": "1.0.0",
  "license": "MIT",
  "dependencies": {
    "json-server": "^0.17.4"
  },
  "scripts": {
    "start": "npx json-server --watch db.json --host 0.0.0.0 --port $PORT"
  }
}